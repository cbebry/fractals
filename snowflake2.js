      //Snowflakes      function snowflake2(n) {         var cos = Math.cos(Math.abs(theta) * deg);         var h = len / (2 * cos + 3);		 scl = h / len;		 ctx.save();         if(n == 0) {            ctx.lineTo(len, 0);         } else {            //to handle recursion.            ctx.scale(scl, scl);            snowflake2(n - 1);            ctx.rotate(theta * deg);			snowflake2(n - 1);            ctx.rotate((-1) * theta * deg);            snowflake2(n - 1);            ctx.rotate((-1) * theta * deg);			snowflake2(n - 1);			ctx.rotate(theta * deg);            snowflake2(n - 1);         }         ctx.restore();		 ctx.translate(len, 0);      }