   var deg = Math.PI / 180;   var completed = 0;   var rotated = 0;   var sign = 1;   var len = 0;   var sides = 0;   var theta = 0;   function cesaro(c, n,x, y, _len, _sides, _theta) {      completed = 0;      var k = 0;      var recursionlevel = 0;	  len = _len;      sides = _sides;	  theta = _theta;	  c.save();	  c.translate(x, y);      c.moveTo(0, 0); //In the base case n = 0,we construct a polygon with equal sides all around...      for(var i = 1; i < sides; i++) {	  ChooseFractal(n, recursionlevel);         c.rotate((-1) * (360 / sides) * deg);      }	  ChooseFractal(n, recursionlevel);   }   function ChooseFractal(n, recursionlevel) {      var selected = $("#fractalSelect").val();      if(selected == "koch") {         snowflake(n);      }      else if(selected == "sierpinski") {	  sierpinski(n);      }      else if(selected == "peano") {	  peano(n);      }      else if(selected == "tree") {	  tree(n, recursionlevel);      }      else if(selected == "cantordust") {	  cantordust(n);      }      else if(selected == "snowflake2") {	  snowflake2(n);	  }      else if(selected == "hilbert") {	  hilbert(n);      }      else if(selected == "gosper") {	  gosper(n);      }      else if(selected == "fern") {	  fern(n);      }}