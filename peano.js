	 //Peano Curve	 function peano(n) {	    var angle = 90;	    var steps = [1];	    for(var i = 0; i < n-1; i++) {	       steps = DoRulesPeano(steps);	    }	    var length = steps.length;	    for(var j = 0; j < length; j++) {	       if(steps[j] > 0) {	          ctx.rotate(-angle * deg);	       } else {	          ctx.rotate(angle * deg);	       }	       ctx.lineTo(len, 0);	       ctx.translate(len, 0);	    }	 }	 function DoRulesPeano(steps) {	    var length = steps.length;	    var newsteps = [];	    for(var i = 0; i < length; i++) {	       if((steps[i]) == 1) {	          newsteps.push(1,1,-1,-1,-1,1,1,1,-1);	       } else if(steps[i] == -1) {	          newsteps.push(-1,1,-1,-1,-1,1,1,1,-1);	    }		}	    return newsteps;	 }