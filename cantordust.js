      function cantordust(n) {         var steps = [1];		 var scale = 1;         var y = 0;         var length = steps.length;		 ctx.lineTo(len, 0);         y += 30;		 ctx.moveTo(0, y);         steps = DoRulesCantor(steps);         for(var i = 0; i < n - 1;i++) {            length = steps.length;            scale = 1 / length;            for(var j = 0; j < length; j++) {               if(steps[j] == 1) {                  ctx.lineTo((j + 1) * scale * len, y);               }               else {                  ctx.moveTo((j + 1) * scale * len, y);               }            }            y += 30;            ctx.moveTo(0, y);            steps = DoRulesCantor(steps);         }      }      function       DoRulesCantor(steps) {         var length = steps.length;         var newsteps = [];         for(var i = 0;i < length; i++) {            if(steps[i] == 1) {               newsteps.push(1, 2, 1);            } else {               newsteps.push(2, 2, 2);            }         }         return newsteps;      }